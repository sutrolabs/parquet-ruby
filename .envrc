# Load devenv's direnv integration from the installed devenv binary. This is
# version-matched to whatever `devenv` is on PATH, so there is no remote fetch
# or pinned hash to keep in sync.
eval "$(devenv direnvrc)"

# RB_SYS_TEST and the .env file are configured in devenv.nix.
use devenv
